# Build output
/yls
/yalazysops
/dist/
/bin/

# Go
*.exe
*.test
*.out
vendor/
go.work.sum

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Local-only — never commit decrypted secrets, ever, even by accident
*.dec.yaml
*.dec.json
*.plaintext
.secrets-runtime/

# AI-assistant per-machine overrides. The shared .claude/settings.json IS
# committed; .claude/settings.local.json is each contributor's personal
# allowlist tweaks and should stay local.
.claude/settings.local.json
